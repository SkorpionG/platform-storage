{
  "extends": "@tooling/typescript-config/react.json",
  "compilerOptions": {
    // `?raw` and the rest of Vite's own module types, which only an app built by Vite can rely on.
    "types": ["vite/client"]
  },
  "include": ["src", "vite.config.ts"]
}
