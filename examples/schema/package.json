{
  "name": "@examples/schema",
  "version": "0.0.0",
  "private": true,
  "description": "The one storage schema every example imports, on every platform.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schema-source": "./src/schema.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "oxlint",
    "clean": "rm -rf .cache .turbo node_modules"
  },
  "dependencies": {
    "@platform-storage/core": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@tooling/oxlint-config": "workspace:*",
    "@tooling/typescript-config": "workspace:*",
    "typescript": "catalog:"
  },
  "engines": {
    "node": ">=20"
  }
}
