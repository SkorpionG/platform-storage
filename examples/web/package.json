{
  "name": "@examples/web",
  "version": "0.0.0",
  "private": true,
  "description": "The browser playground for @platform-storage/web, rendered by every web example.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "oxlint",
    "clean": "rm -rf .cache .turbo node_modules"
  },
  "dependencies": {
    "@examples/schema": "workspace:*",
    "@examples/ui": "workspace:*",
    "@platform-storage/react": "workspace:*",
    "@platform-storage/web": "workspace:*"
  },
  "devDependencies": {
    "@tooling/oxlint-config": "workspace:*",
    "@tooling/typescript-config": "workspace:*",
    "@types/react": "catalog:",
    "react": "catalog:",
    "typescript": "catalog:"
  },
  "peerDependencies": {
    "react": "catalog:"
  },
  "engines": {
    "node": ">=20"
  }
}
